// const getUserName = (id: number): string | null => {
//     if (id === 1) {
//         return "Alamin";
//     } else {
//         return null;
//     }
// };

// console.log(getUserName(1)); // "Alamin"
// console.log(getUserName(2)); // null

const getUserName = (id: number) : string | null => {
    if (id === 1) {
        return "Alamin";
    }
    else {
        return null;
    }
}

// console.log(getUserName(1));
console.log(getUserName(2));